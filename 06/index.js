const distanciaPercorrida = 50; //metros
const tempo = 10; // segundos
const velocidade = (distanciaPercorrida / tempo) * 3.6; // km/h

console.log(`A velocidade média da distância percorrida foi de ${velocidade} km/h`);