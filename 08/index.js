const pi = 3.14159;
const raio = 1;
const comprimentoDoCirculo = 2 * pi * raio
const areaDoCirculo = pi * raio ** 2

console.log(`o Comprimento do circulo é ${comprimentoDoCirculo}`);

console.log(`a área do circulo é ${areaDoCirculo}`);